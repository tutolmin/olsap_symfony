<form method="post" action="{{ path('app_hardware_profiles_delete', {'id': hardware_profile.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hardware_profile.id) }}">
    <button class="btn">Delete</button>
</form>