<form method="post" action="{{ path('app_session_start', {'id': session.id}) }}" onsubmit="return confirm('Are you sure you want to start this session?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ session.id) }}">
    <button class="btn">Start</button>
</form>
