<form method="post" action="{{ path('app_sessions_start', {'hash': session.hash}) }}" onsubmit="return confirm('Are you sure you want to start this session?');">
    <input type="hidden" name="_token" value="{{ csrf_token('start' ~ session.hash) }}">
    <button class="btn">Start</button>
</form>
