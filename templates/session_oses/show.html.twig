{% extends 'base.html.twig' %}

{% block title %}SessionOses{% endblock %}

{% block body %}
    <h1>SessionOses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ session_os.id }}</td>
            </tr>
            <tr>
                <th>Session</th>
                <td>{{ session_os.session }}</td>
            </tr>
            <tr>
                <th>Operating system</th>
                <td>{{ session_os.os }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_session_oses_index') }}">back to list</a>

    <a href="{{ path('app_session_oses_edit', {'id': session_os.id}) }}">edit</a>

    {{ include('session_oses/_delete_form.html.twig') }}
{% endblock %}
