<form method="post" action="{{ path('app_session_oses_delete', {'id': session_os.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ session_os.id) }}">
    <button class="btn">Delete</button>
</form>
