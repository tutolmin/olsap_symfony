<form method="post" action="{{ path('app_task_techs_delete', {'id': task_tech.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ task_tech.id) }}">
    <button class="btn">Delete</button>
</form>
