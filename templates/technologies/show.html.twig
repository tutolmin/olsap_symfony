{% extends 'base.html.twig' %}

{% block title %}Technologies{% endblock %}

{% block body %}
    <h1>Technologies</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ technology.id }}</td>
            </tr>
            <tr>
                <th>Domain</th>
                <td>{{ technology.domain }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ technology.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ technology.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_technologies_index') }}">back to list</a>

    <a href="{{ path('app_technologies_edit', {'id': technology.id}) }}">edit</a>

    {{ include('technologies/_delete_form.html.twig') }}
{% endblock %}
