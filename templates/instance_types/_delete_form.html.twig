<form method="post" action="{{ path('app_instance_types_delete', {'id': instance_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ instance_type.id) }}">
    <button class="btn">Delete</button>
</form>
