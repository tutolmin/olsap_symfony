{% extends 'base.html.twig' %}

{% block title %}SessionStatuses{% endblock %}

{% block body %}
    <h1>SessionStatuses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ session_status.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ session_status.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_session_statuses_index') }}">back to list</a>

    <a href="{{ path('app_session_statuses_edit', {'id': session_status.id}) }}">edit</a>

    {{ include('session_statuses/_delete_form.html.twig') }}
{% endblock %}
