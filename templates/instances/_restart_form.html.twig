<form method="post" action="{{ path('app_instances_restart', {'id': instance.id}) }}" onsubmit="return confirm('Are you sure you want to restart this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('restart' ~ instance.id) }}">
    <button class="btn">Restart</button>
</form>
