{% extends 'base.html.twig' %}

{% block title %}Ports{% endblock %}

{% block body %}
    <h1>Ports</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ port.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ port.number }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ports_index') }}">back to list</a>

    <a href="{{ path('app_ports_edit', {'id': port.id}) }}">edit</a>
<!--
    {{ include('ports/_delete_form.html.twig') }}
-->
{% endblock %}
