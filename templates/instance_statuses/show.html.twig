{% extends 'base.html.twig' %}

{% block title %}InstanceStatuses{% endblock %}

{% block body %}
    <h1>InstanceStatuses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ instance_status.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ instance_status.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_instance_statuses_index') }}">back to list</a>

    <a href="{{ path('app_instance_statuses_edit', {'id': instance_status.id}) }}">edit</a>

    {{ include('instance_statuses/_delete_form.html.twig') }}
{% endblock %}
